# Board

<!-- GENERATED by donefile — do not edit. Edit task files in .donefile/tasks/ and run `donefile board`. For live staleness run `donefile status`. -->

**3 open · 1 claimed · 1 blocked · 0 merged · 35 done · 2 dropped**

## Claimed

- `bt-1402` **Convert bt-a892 off the legacy task-file schema so the bass-tuner board audits clean** — capacity-engine, since 2026-07-26

## Blocked

- `bt-b34a` **Decide whether to flip judge_gates:true in capacity-engine — OMRI'S CALL, do not flip to generate data** — Omri's call — subjective config decision. Main (2026-07-25): must be decided against real measurements, not the void bt-c0a9/bt-e0c3 baseline; do not flip it to generate the data. (since 2026-07-25)

## Open

### p3

- `bt-03ea` **Vercel wildcard cert will likely expire again in ~90d (~2026-10-23) — DNS lives on Cloudflare nameservers, not Vercel's, so Vercel can't auto-place the DNS-01 challenge on renewal, which is probably why it silently expired last time. Needs either a recurring reminder to redo `vercel certs issue "*.omrihefez.com"` before expiry, or a scripted renewal (CF token + vercel CLI, both non-interactive) wired into a cron/donefile task.** — #vercel #tls #ops
- `bt-7cfb` **donefile `done` can't replace/repair evidence on an already-closed task when a --test command fails for reasons unrelated to the underlying claim (e.g. shell quoting) — worth an `evidence add`/`done --amend` escape hatch** — #tooling #donefile
- `bt-b806` **tuner.omrihefez.com serves a full duplicate of bass.omrihefez.com instead of a 308 redirect, violating DOMAIN.md §2 rule 3 (vanity alias must redirect to canonical)** — #vercel #docs

## Done (most recent 15 of 35)

- `bt-73c9` **Close/unblock iac-7aaa referencing bt-e0c3+bt-d2e6+bt-c0a9 as the resolution — it's an orphaned duplicate decision-gate now that the flip already happened and was reviewed** — 2026-07-26 ✓ evidenced
- `bt-cc03` **judge_dedup's "319 skips, 0 judge calls, $0, zero false positives" baseline (bt-c0a9/bt-e0c3) is VOID — it was measured while the judge could not authenticate at all; re-measure and rewrite config.json's _judge_dedup_note and _bt_e0c3_decision_note** — 2026-07-26 ✓ evidenced
- `bt-3943` **Wire CLAUDE_CODE_OAUTH_TOKEN into capacity-engine.service (Infisical, same self-heal pattern as ~/bin/claude-hl) so judge()/judgeDuplicate()/judgeGate() headless calls actually authenticate — currently 100% silently fail-open** — 2026-07-25 ✓ evidenced
- `bt-78e6` **Reconcile ~/meni/DOMAIN.md — tuner re-added to bass-tuner 2026-07-22, registry still shows it sunset** — 2026-07-25 ✓ evidenced
- `bt-2b10` **Vercel fallback cert for unclaimed omrihefez.com subdomains has expired — TLS hard-fails instead of clean 404 (e.g. composer.omrihefez.com)** — 2026-07-25 ✓ evidenced
- `bt-5b65` **Re-check judge_dedup false-positive/cost rate once state.judgeDedupStats shows real calls (journalctl -u capacity-engine.service since 2026-07-23** — 2026-07-25 ✓ evidenced
- `bt-ba37` **Fix digest-note→donefile filing pipeline dropping note body, using the "filed as" annotation as the title instead (repro: bt-f81f / dn-4ab9)** — 2026-07-25 ✓ evidenced
- `bt-e0c3` **Decide whether to flip judge_dedup:true by default in capacity-engine now that the mechanism is built and tested** — 2026-07-25 ✓ evidenced
- `bt-c0a9` **Watch judge_dedup false-positive/cost rate over the next week now that it's default-on across all boards, not just bass-tuner** — 2026-07-25 ✓ evidenced
- `bt-07f2` **Vercel project has no GitHub auto-deploy wired up — pushes to main don't ship; every deploy needs a manual `vercel --prod`** — 2026-07-25 ✓ evidenced
- `bt-a740` **Audit other omrihefez.com subdomains for the same drift (DNS pointed at Vercel but domain never added to a project)** — 2026-07-24 ✓ evidenced
- `bt-d2e6` **bt-ebc4 was a dispatcher duplicate of bt-a66f (same fix, same commit) — same class of issue as bt-6307 (bt-b898/bt-86e6 dup); worth tightening dedup so perf tasks with overlapping descriptions don't get claimed twice** — 2026-07-23 ✓ evidenced
- `bt-c951` **Add GitHub Actions CI to run `npm test` on push** — 2026-07-23 ✓ evidenced
- `bt-cbe8` **No favicon: /favicon.ico 404s every load, no tab/home-screen icon** — 2026-07-23 ✓ evidenced
- `bt-7bc5` **No CI — static site deploys with no lint/validate/link/regression guard** — 2026-07-23 ✓ evidenced
