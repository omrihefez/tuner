# Board

<!-- GENERATED by donefile — do not edit. Edit task files in .donefile/tasks/ and run `donefile board`. For live staleness run `donefile status`. -->

**5 open · 0 claimed · 1 blocked · 0 merged · 57 done · 4 dropped**

## Blocked

- `bt-b34a` **Decide whether to flip judge_gates:true in capacity-engine — OMRI'S CALL, do not flip to generate data** — Omri's call — subjective config decision (flip judge_gates:true in capacity-engine or explicitly keep it false), per Main's 2026-07-25 ruling: must be judged against real measurements, not the void bt-c0a9/bt-e0c3 baseline, and must NOT be flipped just to generate that data. Unblock predicate: a decision record committed at .donefile/evidence/bt-b34a-decision.md (either outcome — flip it, or explicitly keep it off — counts as decided). (since 2026-07-25)

## Open

### p2

- `bt-ed7f` **renew-wildcard-cert.sh (weekly cron) mutates real Cloudflare DNS + issues prod TLS cert from a live, dirty-able checkout — same execute-from-checkout hole as ce-0bda** — #reliability #infra

### p3

- `bt-d428` **install-monitoring-crons.sh's equivalent cleanup pass (grep -vF "run-monitor.sh <name> ") also matches outside its own managed block, just with a narrower pattern than the basename bug this task fixed — not a live bug today but same defect shape if anyone ever hand-adds a line matching that exact string.** — #ops #safety
- `bt-e5ea` **test-monitoring.sh step 5 (live crontab scan) is intermittently flaky on the shared box, unrelated to any installer logic** — #ops #tests
- `bt-4e2a` **Wire probe-bt-5fb7.sh into a periodic cron/monitor so a stale-deploy regression is caught automatically, not only when a task happens to close** — #ops #monitoring
- `bt-5149` **Domain monitor still checks sunset albumclub.omrihefez.com, alarms daily** — #from-brief

## Done (most recent 15 of 57)

- `bt-4923` **renew-wildcard-cert.sh --force performs live ACME issuance with no confirmation guard** — 2026-08-06 ✓ evidenced
- `bt-a60b` **bass-tuner board has no activation gate, so a task on a live Vercel PWA closes done on a commit alone** — 2026-08-05 ✓ evidenced
- `bt-5fb7` **check-sw-cache-bump.js only verifies the git diff at commit time — nothing checks that the deployed sw.js CACHE actually matches what shipped** — 2026-08-05 ✓ evidenced
- `bt-d517` **CI never verifies vercel.json security headers, so an edit can silently drop CSP or frame-ancestors with no test failing** — 2026-08-04 ✓ evidenced
- `bt-a428` **wildcard cert-renewal cron intermittently fails: `vercel: command not found`** — 2026-08-03 ✓ evidenced
- `bt-d30a` **test-monitoring.sh's crontab-scheduled check is flaky under concurrent crontab writes — spurious FAIL despite the entry being present** — 2026-08-01 ✓ evidenced
- `bt-b38f` **install-cert-renewal-cron.sh's cleanup strips ANY crontab line naming renew-wildcard-cert.sh, not just its own block** — 2026-08-01 ✓ evidenced
- `bt-9f09` **Clean up abandoned worktree /tmp/bt-a942-wt2 (branch bt-a942-heartbeat) — stale uncommitted attempt at the heartbeat check, never merged** — 2026-08-01 ✓ evidenced
- `bt-3f5a` **install-cert-renewal-cron.sh rewrites its managed block wholesale with no drop guard — same defect class as bt-34af, currently consistent so no live bug** — 2026-08-01 ✓ evidenced
- `bt-c8b4` **mic-blocked recovery instructions name the pre-redirect domain, so they send the user to look for a permission entry that is not the one blocking them** — 2026-08-01 ✓ evidenced
- `bt-ab14` **check-sw-cache-bump.js still hard-fails HTML and JS changes even though sw.js went network-first for exactly those** — 2026-08-01 ✓ evidenced
- `bt-4ee7` **sw.js cache-first branch has no offline fallback, so an uncached same-origin asset rejects respondWith when the network is down** — 2026-08-01 ✓ evidenced
- `bt-6492` **Nothing checks the heartbeat monitor's own liveness — check-monitor-heartbeats.sh watches the other three monitors but not itself, and it runs from the very cron block it watches** — 2026-08-01 ✓ evidenced
- `bt-34af` **install-monitoring-crons.sh will silently DELETE the heartbeat cron bt-b542 depends on — its managed block omits the entry it now wipes** — 2026-07-31 ✓ evidenced
- `bt-b542` **The heartbeat monitor that watches the other monitors has never existed — cron calls check-monitor-heartbeats.sh daily, exit 127** — 2026-07-31 ✓ evidenced
