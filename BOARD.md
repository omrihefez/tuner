# Board

<!-- GENERATED by donefile — do not edit. Edit task files in .donefile/tasks/ and run `donefile board`. For live staleness run `donefile status`. -->

**14 open · 1 claimed · 0 blocked · 0 merged · 4 done · 1 dropped**

## Claimed

- `bt-86e6` **Add test harness + known-answer tests for pitch math** — capacity-engine, since 2026-07-14

## Open

### p1

- `bt-b898` **No automated tests for core pitch/note math** — #tests

### p2

- `bt-314a` **Network-first SW for HTML/JS to kill stale-app deploys** — #deploy
- `bt-8e75` **Mic stream leaks (stays recording) if AudioContext/analyser setup throws after getUserMedia** — #correctness
- `bt-a66f` **YIN runs on native 44.1/48kHz at fftSize 8192 — ~11M float ops/detection, heavy on mobile** — #perf
- `bt-b0f3` **Service worker is cache-first for HTML/JS — stale app ships unless CACHE is manually bumped** — #deploy
- `bt-ebc4` **Downsample/22.05kHz AudioContext to halve YIN CPU** — #perf

### p3

- `bt-21e7` **Haptic buzz when in tune on mobile (navigator.vibrate)** — #feature
- `bt-4310` **Announce live cents/note readout to screen readers (aria-live)** — #a11y
- `bt-1628` **Reference-tone feedback: played tone leaks into live mic and jerks the needle** — #ux
- `bt-243a` **Docs say 4096-sample window / 22.05kHz but code uses fftSize 8192 at native sample rate** — #docs
- `bt-3a17` **CSP style-src 'unsafe-inline' appears unnecessary — tighten it** — #security
- `bt-7bc5` **No CI — static site deploys with no lint/validate/link/regression guard** — #deploy
- `bt-8db5` **Diagnostic log ships in production DOM and echoes full User-Agent** — #polish
- `bt-cbe8` **No favicon: /favicon.ico 404s every load, no tab/home-screen icon** — #ux

## Done

- `bt-ae2a` **Full audit: bass-tuner — file each finding as its own task (security, correctness, tests, UX, perf, deploy, deps)** — 2026-07-14 ✓ evidenced
- `bt-4e51` **Restore pinch-zoom — drop maximum-scale=1 / user-scalable=no (a11y/WCAG)** — 2026-07-02 ✓ evidenced
- `bt-4f03` **Persist last instrument, tuning & A4 across reloads (localStorage)** — 2026-07-02 ✓ evidenced
- `bt-a892` **Reference tone: tap the locked string to hear its target pitch (Web Audio osc)** — ? ⚠️ NO EVIDENCE
