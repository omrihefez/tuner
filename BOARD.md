# Board

<!-- GENERATED by donefile — do not edit. Edit task files in .donefile/tasks/ and run `donefile board`. For live staleness run `donefile status`. -->

**4 open · 0 claimed · 0 blocked · 0 merged · 71 done · 5 dropped**

## Open

### p1

- `bt-b7e7` **Tuner stopped working on his device — server, deploy and code all verified healthy, cause is client-side** — #from-omri #bug

### p3

- `bt-7654` **install-cert-renewal-cron.sh's cleanup has the same substring-match defect (bt-b38f fixed elsewhere) for its cert-renewal pattern — same grep -vFxf fix would close it** — #ops #safety
- `bt-7964` **Vendored alert-latch.sh has zero consumers here, so every monitor re-alarms daily on an unchanged condition instead of latching once** — #ops #monitoring
- `bt-91e2` **bass-tuner main checkout has a stale staged .donefile/config.yml + bt-3143 evidence-file diff sitting uncommitted since 2026-08-15 (bt-8e12/bt-3143, both already done) — commit or discard it** — #bass-tuner #bookkeeping

## Done (most recent 15 of 71)

- `bt-d5e2` **donefile pre-push hook should scrub GIT_* before running the suite — fleet-wide defense in depth for the bt-f541 worktree-GIT_DIR escape** — 2026-08-22 ✓ evidenced
- `bt-b130` **check-fallback-cert.sh only detects a wildcard cert that has ALREADY expired — it never reads notAfter, so it gives zero lead time on the exact failure it exists for** — 2026-08-18 ✓ evidenced
- `bt-f541` **check-sw-cache-bump.test.js's git isolation is broken: a pre-push npm test run wiped and overwrote production main with test fixture content** — 2026-08-17 ✓ evidenced
- `bt-4b38` **Diagnostic log has no way off the device — no copy, share or beacon, so the on-page panel cannot help the open client-side p1** — 2026-08-15 ✓ evidenced
- `bt-3143` **Service worker falls back to cache only when fetch REJECTS, so a 5xx or a bad-deploy 404 is served to the user instead of the working cached copy** — 2026-08-15 ✓ evidenced
- `bt-8e12` **Update ~/meni/DOMAIN.md albumclub row to sunset/removed (same shape as apartments row, line 25)** — 2026-08-15 ✓ evidenced
- `bt-5149` **Domain monitor still checks sunset albumclub.omrihefez.com, alarms daily** — 2026-08-15 ✓ evidenced
- `bt-4e2a` **Wire probe-bt-5fb7.sh into a periodic cron/monitor so a stale-deploy regression is caught automatically, not only when a task happens to close** — 2026-08-14 ✓ evidenced
- `bt-e5ea` **test-monitoring.sh step 5 (live crontab scan) is intermittently flaky on the shared box, unrelated to any installer logic** — 2026-08-14 ✓ evidenced
- `bt-9777` **install-cert-renewal-cron.sh:143 substring-matches, silently deleting hand-added cert-renewal crontab lines** — 2026-08-14 ✓ evidenced
- `bt-d428` **install-monitoring-crons.sh's equivalent cleanup pass (grep -vF "run-monitor.sh <name> ") also matches outside its own managed block, just with a narrower pattern than the basename bug this task fixed — not a live bug today but same defect shape if anyone ever hand-adds a line matching that exact string.** — 2026-08-14 ✓ evidenced
- `bt-6f67` **bt-b34a is a false gate — ce-5e79 already decided judge_gates and ce-39cd supplied the 80-task live data its unblock predicate demanded** — 2026-08-13 ✓ evidenced
- `bt-b34a` **Decide whether to flip judge_gates:true in capacity-engine — OMRI'S CALL, do not flip to generate data** — 2026-08-13 ✓ evidenced
- `bt-ed7f` **renew-wildcard-cert.sh (weekly cron) mutates real Cloudflare DNS + issues prod TLS cert from a live, dirty-able checkout — same execute-from-checkout hole as ce-0bda** — 2026-08-12 ✓ evidenced
- `bt-4923` **renew-wildcard-cert.sh --force performs live ACME issuance with no confirmation guard** — 2026-08-06 ✓ evidenced
